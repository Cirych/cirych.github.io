/** @type {import("prettier").Config} */
const config = {
  plugins: ['prettier-plugin-tailwindcss'],
  tabWidth: 2,
  singleQuote: true,
  trailingComma: 'es5',
  arrowParens: 'avoid',
};

module.exports = config;
